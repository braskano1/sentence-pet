/** Intro cutscene sources. Empty INTRO_VIDEO_SRC = show the placeholder. */
export const INTRO_VIDEO_SRC = '/intro.mp4'; // H.264 720p, +faststart — universal fallback
export const INTRO_VIDEO_WEBM = '/intro.webm'; // VP9/Opus — smaller where supported
export const INTRO_VIDEO_POSTER = '/intro-poster.jpg'; // first-frame poster (avoids a black flash)
